# V14.7.10 GPU PaddleOCR-VL service dependencies.
# This file intentionally does NOT install torch/torchvision/torchaudio.
# The Dockerfile installs the matched cu126 PyTorch stack AFTER PaddleOCR/PaddleX
# to avoid the mixed CUDA-family torch/torchvision mismatch seen during standalone testing.
flask>=3.0,<4.0
werkzeug>=3.0,<4.0
pillow>=10.0,<12.0
numpy

--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu==3.2.1
paddleocr[doc-parser]>=3.6.0,<4.0.0
paddlex[ocr]

transformers
accelerate
safetensors
tokenizers
sentencepiece
python-docx
