# Core web + parsing
flask==3.1.1
requests==2.32.4
# Keep Requests' transport/encoding detection within its supported ranges.
urllib3>=2.2,<3
charset-normalizer>=3.3,<4
chardet>=5.2,<6
pillow==11.3.0
opencv-python-headless==4.12.0.88
python-dotenv==1.1.1

# Keep NumPy below 2.3 for SciPy/sklearn/paddlex compatibility.
numpy==2.2.6

# PaddleOCR stack for the application OCR runtime.
paddlepaddle==3.2.0
paddleocr>=3.0.0,<4.0.0

# PaddleOCR-VL is mandatory and runs in the separate receipt-vlm service.
# Its GPU-specific dependencies are pinned in requirements/vlm-gpu-cu126.txt.

# RAG-SQL graph orchestration and typed validation.
langgraph==1.2.7
pydantic>=2.7,<3
pytest>=8.2,<10